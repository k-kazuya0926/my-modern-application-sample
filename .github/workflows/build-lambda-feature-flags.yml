name: build-lambda-feature-flags
on:
  push:
    branches: [main]
    paths:
      - applications/feature-flags/**
      - .github/workflows/build-lambda-feature-flags.yml
defaults: # パイプエラーを拾えるようにデフォルトシェルを設定
  run:
    shell: bash
concurrency: # コミット追加時に古いワークフローの実行を自動キャンセル
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    strategy:
      matrix:
        env: [prod] # 用意する環境に応じて変更
    runs-on: ubuntu-latest
    environment: ${{ matrix.env }}
    timeout-minutes: 5
    permissions:
      contents: read
      id-token: write
    env:
      ROLE_ARN: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/${{ github.event.repository.name }}-${{ matrix.env }}-github-actions
      SESSION_NAME: gh-oidc-${{ github.run_id }}-${{ github.run_attempt }}
      ECR_REPOSITORY_URI: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ vars.AWS_REGION }}.amazonaws.com/${{ github.event.repository.name }}-${{ matrix.env }}-feature-flags
    steps:
      - uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.24"
      - name: Run lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.2.2
          working-directory: applications/feature-flags
      - name: Run tests
        run: |
          cd applications/feature-flags
          go test -v ./...
      - uses: aws-actions/configure-aws-credentials@v4 # 一時クレデンシャルの取得
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          role-session-name: ${{ env.SESSION_NAME }}
          aws-region: ${{ vars.AWS_REGION }}
      - uses: ./.github/actions/build-lambda/
        id: build
        with:
          ecr-repository-uri: ${{ env.ECR_REPOSITORY_URI }}
          function-name: feature-flags
      # デプロイまで行う場合は次のようにする
      # - uses: ./.github/actions/deploy-lambda/
      #   with:
      #     function-name: ${{ github.event.repository.name }}-${{ matrix.env }}-feature-flags
      #     image-uri: ${{ steps.build.outputs.image-uri }}
